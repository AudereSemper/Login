import logList from './login.reducers';
import regList from './register.reducers';
import userList from './user.reducers';

const reducers = {
  logList,
  regList,
  userList
}

export default reducers;